# kondukt

> MCP DevTools — test, validate, debug, and scaffold MCP servers.

Status: **pre-release (Phase 1: client SDK foundation)**.

Full documentation, CLI, and npm publish are coming in subsequent phases. See
`docs/prd-core.md` in the parent Kondukt workspace for the full product spec.

## Development

```bash
pnpm install
pnpm build
pnpm test
pnpm lint
pnpm typecheck
```

## License

MIT — see `LICENSE`.
